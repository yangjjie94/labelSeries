

from libs.lib import struct

shapeTypes = struct(
            shape = 'shape',
            box = 'box',
            line = 'line',
            ellipse = 'ellipse',
            polygon = 'polygon'
        )